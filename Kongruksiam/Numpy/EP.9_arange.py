import numpy as np
# arr_a = np.arange(start,stop,step,dtype)
# สร้าง array 1D จาก start ถึง stop โดยนับเป็น step

arr_a = np.arange(0,10,dtype = "int")
print(arr_a)
arr_b =np.arange(1,11,2) # ไม่มี 11 เหมือนตอนใช้ for item in range()
print(arr_b)
